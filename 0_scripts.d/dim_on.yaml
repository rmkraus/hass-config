# Controls physical mode toggle buttons.
# Turns on the dim button and all others off.
sequence:
  - service: homeassistant.turn_on
    entity_id: switch.dim
  - service: homeassistant.turn_off
    entity_id: switch.dim__others
