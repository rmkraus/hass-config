# Controls physical mode toggle buttons.
# Turns on the movie button and all others off.
sequence:
  - service: homeassistant.turn_on
    entity_id: switch.movie
  - service: homeassistant.turn_off
    entity_id: switch.movie__others
